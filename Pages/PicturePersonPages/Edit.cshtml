@page
@model WebProgProject.Pages.PicturePersonPages.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>PicturePerson</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PicturePerson.id" />
            <div class="form-group">
                <label asp-for="PicturePerson.PersonID" class="control-label"></label>
                <select asp-for="PicturePerson.PersonID" class="form-control" asp-items="ViewBag.PersonID"></select>
                <span asp-validation-for="PicturePerson.PersonID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PicturePerson.pictureID" class="control-label"></label>
                <select asp-for="PicturePerson.pictureID" class="form-control" asp-items="ViewBag.pictureID"></select>
                <span asp-validation-for="PicturePerson.pictureID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
